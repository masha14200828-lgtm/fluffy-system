# Core Module

This module contains core business logic for the marketing infrastructure system.

## Structure

- `models/` - Data models and entities
- `services/` - Core service implementations
- `utils/` - Helper functions
